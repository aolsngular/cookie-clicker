import { css } from "lit";

export const commonsStyles = css`
  button {
    width: 100%;
    max-width: 280px;
    border-radius: 8px;
    border: 1px solid transparent;
    padding: 0.6em 1.2em;
    font-size: 16px;
    font-weight: 500;
    font-family: inherit;
    background-color: #007bff;
    text-transform: uppercase;
    cursor: pointer;
    transition: border-color 0.25s;
  }
`;
